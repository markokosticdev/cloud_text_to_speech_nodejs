export class InitParamsMicrosoft {
  subscriptionKey: string;
  region: string;

  constructor(subscriptionKey: string, region: string) {
    this.subscriptionKey = subscriptionKey;
    this.region = region;
  }
}
